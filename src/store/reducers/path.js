const initialState = {

}


const pathReducer = (state = initialState, action) => {
    switch (action.type) {
        case "not":

            break;

        default:
            return state;
    }
}


export default pathReducer;